#include <stdio.h>

int main()
{
    // Define os valores
    int raiz;
    int number1 = 10;

    // Executa o calculo
    raiz = number1 * number1;

    // E define a saida de dados
    printf("A raiz do numero %d e: %d", number1, raiz);

    return 0;
}
